import {useDispatch} from "react-redux";import {minusCounter, plusCounter, resetCounter} from "../reducers/counter";const Counter =() => {    const dispatch = useDispatch()    return (        <div className="btn-group">            <button className='btn btn-success' onClick={()=> dispatch(plusCounter())}>Plus</button>            <button className='btn btn-secondary' onClick={()=> dispatch(minusCounter())}>Minus</button>            <button className='btn btn-danger' onClick={()=> dispatch(resetCounter())}>Reset</button>        </div>    )}export  default Counter